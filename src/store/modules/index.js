import appsModule from './apps.store';
import usersModule from './users.store';
import attrsModule from './attrs.store';
import graphsModule from './graphs.store';

const modules = {
    appsModule, usersModule, attrsModule, graphsModule
};

export default modules;